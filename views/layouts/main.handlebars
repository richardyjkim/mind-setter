<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.3/css/all.css"
    integrity="sha384-SZXxX4whJ79/gErwcOYf+zWLeJdY/qpuqC4cAa9rOGUstPomtqpuNWT9wdPEn2fk" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
  <title>Mind-Setter</title>
</head>

<body>
  <div class="wrapper">
    <header class="hero">
      <div>
        <nav class="nav">
          <a class="link" href="/dashboard"><i class="fas fa-heart"></i>Mindset</a>
          {{#if loggedIn}}
          <button class="link" id="logout"><i class="fas fa-heart-broken"></i>Logout</button>
          {{else}}
          <a class="link" href="/login"><i class="fas fa-heart"></i>Login</a>
          <a class="link" href="/signup"><i class="fas fa-hand-holding-heart"></i>Sign-up</a>
          {{/if}}
        </nav>
      </div>

      <h1>
        <a class="app-title" href="/"><i class="fas fa-heartbeat">Mind-Setter</i></a>
      </h1>
    </header>
    <main>
      {{{ body }}}
    </main>
  </div>
  <br>
  <footer class="footer">
    “The mind is not a vessel to be filled, but a fire to be kindled.”
  </footer>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>